# The handle is built from a bare hostname, so a URL here would federate the
# site as `@blog@https://example.com`.
[activitypub]
domain = "https://example.com/"

[activitypub.objects.post]
content = "body"
